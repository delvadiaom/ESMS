/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SingleUserActivityLogComponent } from './single-user-activity-log.component';
import { MAT_DIALOG_DATA, MatDialogModule, MatDialogRef } from '@angular/material/dialog';
import { MatTableModule } from '@angular/material/table';
import { HttpClientModule } from '@angular/common/http';

describe('SingleUserActivityLogComponent', () => {
  let component: SingleUserActivityLogComponent;
  let fixture: ComponentFixture<SingleUserActivityLogComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ SingleUserActivityLogComponent ],
      imports:[MatDialogModule,MatTableModule,HttpClientModule],
      providers: [
        { provide: MAT_DIALOG_DATA, useValue: {} },
        { provide: MatDialogRef, useValue: {} }
    ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(SingleUserActivityLogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
 */